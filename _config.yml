title: style revolution plates
description: >- #
baseurl: "/srplates" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown
theme: minima
plugins:
  - pagemaster

exclude: [Gemfile*, node_modules, vendor]

collections:
  plates:
    output: true
    pm_generate: false
    pm_input: csv
    pm_source: plates
    pm_key: plate
    pm_directory: plates
    pm_layout: plate-page
